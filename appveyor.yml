image: Visual Studio 2015

environment:
  matrix:
  - QT: C:\Qt\5.10.1\msvc2017_64
    PLATFORM: amd64
    COMPILER: msvc
    VSVER: 14

install:
  - set QTDIR=C:\Qt\5.9\mingw53_32
  - set PATH=%PATH%;%QTDIR%\bin;C:\MinGW\bin

build_script:
  - call "appveyor/build_windows.cmd"

 # - cd VisualRacing\VRMain
 # - qmake -o Makefile VRMain.pro -spec win32-g++ "CONFIG+=debug" "CONFIG+=qml_debug"
 # - mingw32-make -f Makefile

#test_script:
#  - cd VisualRacing/VRTest
#  - qmake VRTest.pro
#  - mingw32-make
#  - dir
#  - echo "Running Tests"
#  - VRTest.exe -xunitxml > tests.xml

#on_finish:
#  - ps: (New-Object System.Net.Webclient).UploadFile("https://ci.appveyor.com/api/testresults/xunit/$($env:APPVEYOR_JOB_ID)", (Resolve-Path .\tests.xml))